#include "main.h"
/**
 *  print_last_digit - last digit
 *
 *@n: The int to print
 * Return: the value of the last digit
 */

int print_last_digit(int n)
{
	if (a > 0)
		return (a % 10);
	else
		return (-1 * a % 10);
}
